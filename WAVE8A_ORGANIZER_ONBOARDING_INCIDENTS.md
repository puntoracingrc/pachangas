# Wave 8A Organizer Onboarding Incidents

Registro permanente de incidencias encontradas durante la implementación y la simulación de Wave 8A. Ninguna incidencia se considera cerrada sin reproducir el escenario original y ejecutar su regresión.

| ID | Clasificación | Estado | Incidencia | Corrección | Regresión |
| --- | --- | --- | --- | --- | --- |
| W8A-001 | PRODUCT_BUG | fixed + regression_verified | `rate_limit.override` acepta contractualmente `application.*`, pero el `CHECK` inicial de `action_pattern` rechazaba `*`. La ruta de override documentada no podía persistirse. | La migración de hardening admite únicamente acciones concretas o un wildcard final `.*`, sin modificar migraciones históricas. | El runner persiste overrides `application.*` y completa las doce carreras reales con cleanup `PASS`. La suite SQL añade la prueba explícita por RPC. |
| W8A-002 | ENVIRONMENT_ISSUE | fixed + regression_verified | El primer runner de concurrencia intentó terminar una conexión interna superuser de Supabase al limpiar su base efímera y el error de cleanup ocultó el fallo original. | El cleanup espera el cierre interno, termina solo clientes no-superuser y conserva el error primario. | `tests/organizer-access-onboarding-v1-concurrency.mjs` termina con `cleanup: PASS` y no deja base efímera. |
| W8A-003 | TESTABILITY_GAP | fixed + regression_verified | Un test TS aislado no podía importar un módulo marcado `server-only` fuera del runtime Next.js. | La regresión valida el contrato del módulo mediante inspección focalizada, sin relajar `server-only`. | `tests/organizer-access-onboarding-v1.test.ts` pasa 16/16. |
| W8A-004 | TESTABILITY_GAP | fixed + regression_verified | La prueba de autoridad de Demo World genera UUID y timestamps volátiles; el hash bruto cambia entre ejecuciones aunque la proyección canónica sea idéntica. | La verificación separa el hash bruto de los hashes de proyección determinista para autoridad y snapshot, y conserva `remoteWrites = 0`. | `npm run demo-world:v2:verify` compara ambas proyecciones con el snapshot versionado e informa sus hashes por separado. |
| W8A-005 | NEEDS_PRODUCT_DECISION | open | El producto tiene lifecycle canónico para Club (`operational_status`) y para cuentas de usuario, pero `pachanga_groups` no dispone de un estado operativo canónico de equipo. Por tanto, “Team suspendido” no puede distinguirse de owner suspendido, billing inactivo o equipo sin actividad. | No se inventa un booleano ni se reutiliza billing como sanción. Club suspendido y actor sin autoridad sí quedan bloqueados; la suspensión propia de Team requiere un contrato futuro transversal. | La suite negativa cubre Club suspendido y actor sin autoridad. El caso Team suspendido queda explícitamente no ejecutable hasta definir su fuente de verdad. |
| W8A-006 | PRODUCT_BUG | fixed + regression_verified | El trigger inicial comparaba los nombres internos `application.submitted` y `application.information_provided`, pero los eventos persistidos conservan las acciones canónicas `application.submit` y `application.respond_information`. Los revisores no habrían recibido esos dos avisos. | El fan-out usa ahora las acciones canónicas reales, separa los once hitos contractuales y añade un worker `service_role` idempotente para accesos próximos a expirar. | La suite SQL prueba destinatarios, obligatoriedad, semántica, replay y deduplicación; la suite TS comprueba el worker y su conexión con ambos endpoints internos. |
| W8A-007 | SIMULATION_BUG | fixed + regression_verified | La prueba negativa del worker de avisos esperaba alcanzar su guardia interna `service_role`, pero el contrato de privilegios bloquea antes la ejecución a `authenticated`. | La aserción exige el error PostgreSQL `permission denied`, que es la barrera efectiva y más restrictiva. | El runner SQL confirma además que solo `service_role` conserva `EXECUTE` y que el worker rechaza cualquier bypass de cliente. |
| W8A-008 | TESTABILITY_GAP | fixed + regression_verified | El gate global de Control Center solo reconocía capabilities formados por letras y puntos, por lo que rechazaba la nueva página aunque invocaba correctamente `requirePlatformPage("organizer_access.read")`. | El patrón admite también `_`, manteniendo obligatoria la autorización server-side en todas las páginas y APIs. | El test focalizado de Platform Control Center reproduce la ruta nueva y la batería global vuelve a comprobar todo el árbol administrativo. |
| W8A-009 | ENVIRONMENT_ISSUE | fixed + regression_verified | El ledger de la rama Supabase de staging contenía siete migraciones Wave 7C con timestamps distintos a los canónicos del repositorio, aunque sus nombres y SQL eran equivalentes salvo un `;` final. Esto impedía aplicar Wave 8A sin reconciliar primero el historial. | Se descargaron las versiones remotas, se comparó cada pareja, se marcaron únicamente esas entradas equivalentes como revertidas y se registraron las siete versiones canónicas como aplicadas. No se alteró el esquema ni se reescribió SQL ya ejecutado. | `supabase migration list --linked` quedó alineado en 197 versiones antes del push; tras aplicar Wave 8A, el readback remoto confirma ledger 204 y exactamente las siete migraciones `20260829152223` a `20260829152250`. |
| W8A-010 | ENVIRONMENT_ISSUE | fixed + regression_verified | Tras aplicar correctamente las siete migraciones, Supabase CLI emitió un aviso de caché `pg-delta` porque no encontró su certificado temporal local. El aviso apareció después de completar el push y podía inducir a repetirlo. | No se reintentaron migraciones. Se trató el aviso como no autoritativo y se exigieron ledger, flags, conteos, ACL y consulta remota directa como confirmación. | El readback remoto confirma ledger 204, siete nombres exactos, flags OFF, cero filas Wave 8A y ausencia de privilegios directos para `anon` y `authenticated`. |
| W8A-011 | ENVIRONMENT_ISSUE | fixed + regression_verified | `supabase db query --linked` envía los archivos mediante Management API y no interpreta el metacomando `psql` `\\set ON_ERROR_STOP`; el primer ensayo transaccional fue rechazado antes de ejecutar datos. | El harness remoto temporal elimina únicamente metacomandos, conserva `BEGIN`/`ROLLBACK` y ejecuta el mismo fixture y cuerpo SQL. | El ensayo corregido avanzó por toda la historia hasta el launcher; tras el fallo posterior, el rollback remoto confirmó cero usuarios, Clubs, equipos, solicitudes, receipts, eventos y workspaces QA persistidos. |
| W8A-012 | SIMULATION_BUG | fixed + regression_verified | El ensayo remoto batched marcó como `scheduled` un grant recién aprobado y `competition.launch` respondió `LEAGUE_PRIVATE_BETA_GRANT_REQUIRED`. Management API envía el archivo completo como una sola sentencia: `statement_timestamp()` queda fijado al inicio del batch y precede al `clock_timestamp()` usado al crear el grant. En llamadas RPC HTTP reales cada operación usa una sentencia nueva. | El harness transaccional ajustó únicamente su grant QA a un instante efectivo anterior. El producto, sus funciones y tiempos canónicos no cambiaron. | El rollback SQL remoto terminó en `ORGANIZER_ACCESS_ONBOARDING_V1_DB_PASS`; la QA HTTP autenticada separó las sentencias, creó el launcher canónico y canceló el wizard. |
| W8A-013 | ENVIRONMENT_ISSUE | fixed + regression_verified | La primera llamada de preparación del E2E autenticado a Supabase Auth staging respondió `401 Invalid API key` porque `vercel env pull` había redactado el secreto branch-scoped como `[SENSITIVE]`. El fallo ocurrió antes de crear usuarios o datos QA. | La sesión Supabase CLI autenticada transfirió la clave `service_role` vigente solo a un runtime temporal `0600`; no se cambió Vercel, no se imprimió el valor y el JSON bruto se eliminó inmediatamente. | Auth admin creó las cuentas efímeras y seis clientes autenticados completaron el E2E. La retirada completa de la rama staging elimina usuarios y evidencias QA al cerrar la release. |
| W8A-014 | ENVIRONMENT_ISSUE | fixed + regression_verified | El primer intento de transferir de forma local y redactada la clave de staging desde Supabase CLI a un archivo temporal combinó una tubería con un heredoc. Ambos competían por `stdin` y el parser recibió una entrada vacía; no se leyó ni se imprimió ninguna credencial. | La captura CLI y el parser se separaron en dos pasos con archivos `0600`, salida solo de metadatos redactados y eliminación inmediata del JSON bruto. | El runtime reportó modo `600`; la QA autenticada terminó sin filtrar secretos en consola, Git o informes. |
| W8A-015 | ENVIRONMENT_ISSUE | fixed + regression_verified | La primera QA HTTP autenticada intentó habilitar `League Private Beta` en una rama de staging donde la Foundation y sus dependencias estaban todas `OFF`. La restricción `pachanga_comp_foundation_private_beta_creation_check` rechazó correctamente ese estado imposible antes de crear solicitudes Wave 8A. | El harness activa temporalmente, por RPC de plataforma y en orden, Foundation, Participation, Scheduling, Match Operations, Operational Exceptions, Private Beta y Wizard V2; restaura sus snapshots previos en orden inverso. | El E2E completo pasó y el readback final confirmó `dependencyFlagsRestored=true`; no se usó `UPDATE` directo. |
| W8A-016 | TESTABILITY_GAP | fixed + regression_verified | La QA HTTP creó y actualizó una solicitud con dos sesiones, pero la segunda sesión no recibió en 30 segundos la invalidación esperada de `pachanga_organizer_access_invalidations_v1`. | Se separaron publicación, fila, RLS y listener; el harness pasó a usar la suscripción sin filtro del cliente real, dejando el ámbito a RLS, y se corrigió el privilegio roto descrito en W8A-018. | El segundo dispositivo recibió la invalidación y ambos clientes obtuvieron snapshots canónicos idénticos; el resultado declara `invalidation_then_canonical_refetch`. |
| W8A-017 | SIMULATION_BUG | fixed + regression_verified | La primera consulta de diagnóstico Realtime asumió que `pachanga_organizer_access_invalidations_v1` tenía una columna `id`; PostgreSQL rechazó la lectura con `42703` y no modificó datos. | El readback usa `scope_key`, `entity_revision` y `server_sequence`, que son las columnas canónicas reales. | La consulta confirmó fila, publicación, política, revisión y secuencia; el E2E posterior recibió esa invalidación. |
| W8A-018 | PRODUCT_BUG | fixed + regression_verified | La lectura autenticada de `pachanga_organizer_access_invalidations_v1` falló con `42501 permission denied for function pachanga_organizer_access_invalidation_can_read_v1`. La política RLS invoca esa función privada, pero la migración había revocado su ejecución a `authenticated`; por ello Realtime tampoco podía autorizar ni entregar el evento al owner. | La migración concede únicamente `EXECUTE` sobre el predicado booleano de RLS a `authenticated`, conservando tabla en solo lectura, RLS, `SECURITY DEFINER`, búsqueda cerrada y toda escritura en autoridad servidor. El mismo `GRANT` focalizado se aplicó en staging. | Owner lee una invalidación, outsider lee cero, `anon` no ejecuta el predicado y `authenticated` no tiene `INSERT`; Realtime, suite TS y SQL/RLS pasan. |
| W8A-019 | SIMULATION_BUG | fixed + regression_verified | El E2E autenticado completó Realtime, privacidad, revisión, launcher y revocaciones, pero el harness llamó a `transfer_pachanga_group_ownership_authoritative_v1` con nombres de argumentos antiguos. PostgREST respondió `PGRST202` y devolvió la firma canónica vigente. | El harness usa `target_group_id`, `target_user_id` y `expected_revision`; no se añadió compatibilidad SQL. | El nuevo owner continuó y retiró la solicitud, el anterior perdió acceso y el readback confirmó el owner esperado. |
| W8A-020 | SIMULATION_BUG | fixed + regression_verified | Al repetir el E2E sobre el mismo Club ya aprobado en el intento anterior, una nueva aprobación devolvió grant pero no un segundo `onboarding` embebido. El workspace es único por organizador y el intento anterior ya tenía launcher. | La regresión final usa usuarios, Club y equipos nuevos en lugar de reutilizar agregados contaminados; no se debilitó la unicidad del workspace. | La primera aprobación limpia creó exactamente un onboarding y un launcher; el wizard quedó cancelado y no hubo duplicado. |
| W8A-021 | SIMULATION_BUG | fixed + regression_verified | El primer readback de la repetición buscó `source_application_id` en `pachanga_competition_entitlement_grants`, columna que no pertenece a ese esquema. PostgreSQL rechazó la consulta completa con `42703`; no hubo escritura. | El diagnóstico usa columnas verificadas por `information_schema` y la relación real `source_access_grant_id` del workspace. | El inventario distinguió aplicaciones, workspace, access grant y proyección de entitlements sin error. |
| W8A-022 | SIMULATION_BUG | fixed + regression_verified | El primer readback agregado de cleanup asumió `user_id` como columna destinataria de `pachanga_user_notifications`; la tabla usa `recipient_user_id`. | La consulta usa el esquema confirmado y no modifica notificaciones. | El readback completo terminó con `accessGrantsActive=0`, `entitlementsActive=0`, dos wizards cancelados y 93 avisos QA trazables. |
| W8A-023 | ENVIRONMENT_ISSUE | fixed + regression_verified | El runner SQL local y el runner de concurrencia se lanzaron en paralelo; ambos restauraron infraestructura PostgreSQL al mismo tiempo y el primero falló antes de cargar Wave 8A con `tuple concurrently updated`. | Los runners que restauran infraestructura compartida se ejecutan en serie; no cambió SQL ni se relajaron aserciones. | Concurrencia terminó 13/13 con cleanup y el runner SQL aislado terminó ledger 204, siete migraciones, RLS/idempotencia y equivalencia de esquema `PASS`. |
| W8A-024 | ENVIRONMENT_ISSUE | fixed + regression_verified | El primer orquestador de activación productiva intentó generar el `operationId` con `crypto.randomUUID()` dentro del isolate de herramientas, donde ese global no está disponible. Falló localmente antes de invocar Supabase; el readback productivo continuó en revisión 1 y con todos los flags `OFF`. | Las seis activaciones usan UUID v4 preasignados, únicos y estables por fase, preservando replay e idempotencia sin depender del runtime del orquestador. | Las seis fases avanzaron de revisión 1 a 7, con readback canónico independiente y exactamente seis receipts de plataforma. |
| W8A-025 | SIMULATION_BUG | fixed + regression_verified | El primer SQL de activación ordenó el único `platform_owner` activo por `created_at`, pero la tabla canónica usa `granted_at`. PostgreSQL devolvió `42703` dentro de la transacción y no llegó a invocar la RPC. | El actor se selecciona por `granted_at, user_id`, sin exponer su identidad ni alterar roles. | La fase 1 avanzó de revisión 1 a 2 y creó un único receipt; las cinco fases siguientes conservaron la progresión monotónica hasta revisión 7. |
| W8A-026 | SIMULATION_BUG | fixed + regression_verified | El primer bucle de smoke HTTP usó `path` como variable en `zsh`; ese nombre está enlazado al `PATH` del shell y dejó `curl` fuera de resolución antes de realizar las peticiones. | El smoke usa `route` como variable y exige código HTTP explícito para cada superficie. | Las ocho superficies de producto, el manifest y el Service Worker devolvieron `200` en `pachangasiq.com`. |
| W8A-027 | ENVIRONMENT_ISSUE | open | El entorno no expone el binario `agent-browser` anunciado por la skill; el comando terminó con `command not found` antes de abrir una página. | Preparada: realizar el mismo smoke con el navegador integrado y conservar la validación HTTP independiente. | Pendiente: navegación, responsive, consola e imágenes en producción. |
| W8A-028 | ENVIRONMENT_ISSUE | open | El fallback `import("playwright")` del REPL resolvió el paquete local, pero falló por una incompatibilidad ESM antes de lanzar Chromium. | Preparada: reutilizar las sesiones persistentes del Browser/Chrome Plugin y limitar el acceso a navegación y DOM, sin leer cookies ni almacenamiento. | Pendiente: navegación, responsive, consola e imágenes en producción. |
| W8A-029 | PRODUCT_BUG | open | La primera recarga autenticada del Control Center con Wave 8A activo emitió React `#418`, señal de hidratación desigual entre HTML servidor y cliente. Los siete flags sí aparecieron activos y no hubo overflow ni imágenes rotas. | Pendiente de aislar; se revisarán únicamente los textos no deterministas de la superficie Organizer Access. | Pendiente: reproducir en local/producción, corregir la causa mínima y obtener cero errores en la recarga original. |

## Criterio de cierre

Cada fila debe terminar como `fixed + regression_verified`, o permanecer abierta con causa y efecto explícitos. No se silencian fallos para continuar el release.
