# Wave 8B Team Operational State Incidents

Registro permanente de incidencias encontradas durante Wave 8B. Ninguna incidencia se corrige silenciosamente: debe clasificarse antes de la correccion y solo puede cerrarse tras reproducir el escenario original y verificar su regresion.

La validacion de esta fase es exclusivamente Demo-only: Simulation World aislado, Demo World saneado y canary productivo sintetico con `ROLLBACK` o cleanup completo. No se utilizan Clubs, equipos, arbitros, organizadores ni usuarios reales.

| ID | Clasificacion | Estado | Incidencia | Correccion | Regresion |
| --- | --- | --- | --- | --- | --- |
| W8B-ENV-001 | ENVIRONMENT_ISSUE | fixed + regression_verified | El primer script efimero de QA visual mezclaba CommonJS con `top-level await` bajo Node 24 y termino antes de abrir Chromium. No realizo escrituras ni modifico producto. | El arnes efimero encapsulo la ejecucion en una funcion `async` y conservo el mismo corpus de viewports. | La matriz Preview completa paso en 1440x900, 1920x1080, 390x844, 360x800, 667x375, 740x360, 844x390, 932x430 y PWA standalone, sin overflow raiz, imagenes rotas, errores de consola online ni exposicion privada. |
| W8B-ENV-002 | ENVIRONMENT_ISSUE | fixed + regression_verified | El branch Supabase efimero intento reproducir el historial legado absorbido y se detuvo en la migracion 10 porque `public.pachanga_admin_invites` todavia no existia. El branch quedo `MIGRATIONS_FAILED`, con tres tablas sinteticas y sin datos productivos. | Se reconstruyo unicamente el branch aislado desde el baseline canonico `20260731080738`, se registraron sus 36 versiones absorbidas y se aplicaron las 176 migraciones posteriores. No se reparo ni reescribio el ledger productivo. | Dry-run exacto de 176 incrementales y push completo; readback inicial confirma ledger aislado 212, ultima `20260829221312`, 245 relaciones publicas, 132 privadas y ausencia de esquema `simulation`. |
| W8B-ENV-003 | ENVIRONMENT_ISSUE | fixed + regression_verified | La URL PostgreSQL directa del branch resolvio a IPv6 y el entorno local no dispone de ruta IPv6. El intento termino antes de autenticar o ejecutar SQL. | Se uso el session pooler IPv4 del mismo branch efimero para el CLI y su transaction pooler solo para consultas compatibles. | Preflight y readback identificaron usuario y base aislados con 36 versiones antes del push; el push termino en 212 sin fallback ni conexion al project ref productivo. |
| W8B-ENV-004 | ENVIRONMENT_ISSUE | fixed + regression_verified | El primer ajuste de configuracion temporal incluyo un comentario con acentos graves dentro de una cadena de shell; `zsh` intento interpretarlo y el comando fallo antes de conectar con PostgreSQL. | La configuracion temporal se construye mediante copia y sustitucion mecanica, sin interpolar Markdown ni secretos; el trap elimina el directorio temporal. | Los intentos posteriores alcanzaron correctamente el preflight del CLI y no dejaron configuracion, credenciales ni archivos temporales persistentes. |
| W8B-ENV-005 | ENVIRONMENT_ISSUE | fixed + regression_verified | El repositorio desactiva deliberadamente `[db.migrations]` y el primer `db push` temporal omitio las migraciones; al habilitarlas de forma efimera, el CLI rechazo correctamente la divergencia entre las diez versiones legadas del branch y el baseline consolidado. Ningun intento aplico esquema. | Se restablecio solo el branch sintetico y se ejecuto el baseline mas incrementales desde una configuracion temporal con migraciones habilitadas. La configuracion versionada permanece intacta. | Dry-run 176/176, push completo, ledger 212 y `git diff` sin cambios en `supabase/config.toml`; el directorio temporal se elimino por `trap`. |
| W8B-ENV-006 | ENVIRONMENT_ISSUE | fixed + regression_verified | Tras aplicar correctamente el baseline y registrar las 36 versiones absorbidas, el `db push --dry-run` sobre el transaction pooler termino con `42P05 prepared statement already exists`. No se aplico ninguna incremental. | Se uso el session pooler IPv4 del mismo branch para las operaciones del Supabase CLI que necesitan prepared statements; el transaction pooler queda reservado a consultas compatibles. | Preflight de identidad del branch, dry-run completo y 176 incrementales aplicadas una sola vez; ledger final 212. |
| W8B-ENV-007 | ENVIRONMENT_ISSUE | fixed + regression_verified | Despues de aplicar las 176 incrementales, Supabase CLI aviso que no podia cachear el catalogo `pg-delta` porque faltaba el certificado temporal `pgdelta-target-ca.crt`. El aviso aparecio despues de `Finished supabase db push`. | No se repitio el push ni se reparo historial. El cache experimental no se usa como autoridad; se exige readback PostgreSQL independiente del ledger, objetos, flags y ACL. | El proceso termino con codigo 0 y el readback independiente confirma ledger 212 y ultima migracion Wave 8B. Las comprobaciones contractuales remotas completaran la evidencia. |
| W8B-TEST-008 | TESTABILITY_GAP | fixed + regression_verified | El primer readback posterior al push busco la relacion supuesta `public.pachanga_team_operational_states_v1`; la autoridad real es privada y plural. El falso negativo no modifico datos. | Se resolvieron los nombres desde las migraciones versionadas y se consulto `private.pachanga_team_operational_states_v1` junto con los contratos publicos exactos. | Readback remoto: tabla privada e invalidacion publica presentes, flags 8/8 `OFF`, cero agregados, RLS activa, publicacion Realtime, 13/13 indices validos y ninguna escritura directa para `authenticated`. |
| W8B-TEST-009 | TESTABILITY_GAP | fixed + regression_verified | El primer inventario remoto de RPC encontro 8 de 9 firmas esperadas porque una identidad textual asumida no coincide con la declaracion PostgreSQL real. Fue una consulta de solo lectura. | Se resolvieron nombres y `pg_get_function_identity_arguments` desde `pg_proc`; la lista de Control Center usa `(filters jsonb, page_limit integer, page_offset integer)`. | 9/9 RPC publicas localizadas. Lecturas publicas limitadas a flags y proyeccion segura; comandos requieren `authenticated`; expiracion solo `service_role`; `anon` no ejecuta comandos ni lecturas privadas. |
| W8B-010 | PRODUCT_BUG | fixed + regression_verified | El Advisor de rendimiento del esquema remoto detecto 21 claves foraneas Wave 8B sin indice de cobertura. La escala funcional pasaba, pero borrados o cambios de referencias de Auth, Competition, Appeal y Restriction podian recorrer tablas completas. | Se anadieron los 21 indices de cobertura a la octava migracion de hardening, sin crear una novena migracion ni modificar las 204 historicas. | La regresion contractual exige los 21 nombres; staging contiene los 39 indices Wave 8B esperados, todos validos y listos. Advisor ya no devuelve `unindexed_foreign_keys` para objetos Team Operational. Suite DB, concurrencia y escala vuelven a pasar. Los avisos `unused_index` son esperables en un branch vacio. |
| W8B-ENV-011 | ENVIRONMENT_ISSUE | fixed + regression_verified | La reconstruccion del branch ya completo intento retirar los esquemas de producto en una unica transaccion y PostgreSQL aborto con `out of shared memory / max_locks_per_transaction` al alcanzar mas de mil objetos dependientes. El rollback conservo el ledger 212 y el esquema anterior intactos. | Se abandono el borrado monolitico. Tras confirmar cero usuarios, grupos y objetos de Storage, la limpieza retiro solo objetos de producto en operaciones granulares y preservo `auth`, `storage`, `realtime`, extensiones e infraestructura. | La limpieza granular dejo ledger 0 y cero relaciones de producto; despues se reconstruyo baseline + 176 incrementales. Readback final: ledger 212, ultima `20260829221312`, cero usuarios/grupos/Storage y ningun esquema `simulation`. |
| W8B-ENV-012 | ENVIRONMENT_ISSUE | fixed + regression_verified | El reset nativo solicitado hasta la migracion legada 10 volvio a ejecutar la cadena historica no reproducible, termino en `MIGRATIONS_FAILED` y dejo intactos el ledger 212 y el esquema previo. No se perdieron datos porque el branch es sintetico y vacio. | No se volvio a usar el replay historico roto como autoridad. Se aplico la reconstruccion canonica desde el baseline consolidado y se registraron sus 36 versiones absorbidas antes de las 176 incrementales. | Readback PostgreSQL independiente confirma las ocho migraciones Wave 8B exactas, flags 8/8 `OFF`, RLS, Realtime, cero escrituras directas autenticadas y todos los agregados sinteticos a cero. El estado historico `MIGRATIONS_FAILED` del recurso no se interpreta como estado del esquema. |
| W8B-TEST-013 | TESTABILITY_GAP | fixed + regression_verified | El primer readback de dependencias de staging supuso el nombre `private.pachanga_public_competition_settings_v1`, que no existe. Tras resolver la relacion real, un segundo intento supuso una columna singleton `id` que tampoco existe. PostgreSQL rechazo ambas consultas completas antes de devolver datos y no se produjo ninguna escritura. | Se resolvieron relaciones y columnas desde `pg_catalog`. El readback usa `private.pachanga_organizer_access_settings_v1` y `private.pachanga_competition_foundation_settings`, ambas con clave `singleton`. | El readback corregido devolvio revisiones 1 y todas las dependencias heredadas `OFF`. La regresion del arnes rechaza los nombres supuestos y exige las RPC productivas reales. |
| W8B-014 | ENVIRONMENT_ISSUE | fixed + regression_verified | En la primera QA autenticada del branch aislado, el canal Realtime alcanzo `SUBSCRIBED` pero no entrego la invalidacion Team en 45 segundos. La RPC de apertura de review ya habia confirmado la revision; no se reintento ni se interpreto el timeout como exito. | El arnes mantiene el refetch canonico inmediato al alcanzar `SUBSCRIBED` y espera la estabilizacion del slot del branch frio antes de emitir la operacion. Realtime solo invalida; nunca aplica WAL como autoridad. | La ejecucion posterior recibio la invalidacion de revision 2 en la segunda sesion, sin campos privados, y el refetch RPC convergio a `UNDER_REVIEW` con la revision confirmada. |
| W8B-TEST-015 | TESTABILITY_GAP | fixed + regression_verified | La primera consulta de diagnostico Realtime supuso una columna `invalidation_kind` que no existe. PostgreSQL rechazo el readback completo y no modifico datos. | Se resolvio la forma de `public.pachanga_team_operational_invalidations_v1` desde `information_schema`; el evento versionado usa `event_kind`. | El readback corregido encontro la invalidacion persistida con revision 2 y `team.state.changed`. La regresion del arnes prohibe `invalidation_kind`. |
| W8B-TEST-016 | TESTABILITY_GAP | fixed + regression_verified | La segunda QA autenticada avanzo mas alla de Realtime y despues intento leer `public.pachanga_groups` directamente mediante `service_role`. El branch denego correctamente `SELECT`; el arnes habia supuesto un privilegio que el producto no concede. | No se amplio ACL. Fixture y readbacks usan PostgreSQL del branch aislado; `service_role` queda limitado a Auth y RPC concedidas. | La siguiente ejecucion alcanzo y verifico historia preservada, guards reales de Mercado y Retos, transferencia, apelaciones y caducidad sin ninguna llamada `service.from()` ni escritura directa de cliente. |
| W8B-TEST-017 | TESTABILITY_GAP | fixed + regression_verified | La tercera QA confirmo la limitacion SOCIAL_ONLY, pero el arnes intento leer `limited.snapshot.state.effectiveStatus`; la respuesta canonica no tiene esa ruta y produjo `TypeError` despues del commit. | Se resolvio la forma desde el receipt persistido y se usan los campos planos `snapshot.effectiveStatus`, `snapshot.lifecycle` y `snapshot.enforcement`. | La ejecucion posterior valido LIMITED, SUSPENDED, archive/restore, independencia Billing/Conduct y llego al bloque final de concurrencia. La regresion estatica prohibe `snapshot.state`. |
| W8B-TEST-018 | TESTABILITY_GAP | fixed + regression_verified | La revision preventiva del tramo pendiente encontro que el arnes preparaba la transferencia de owner con el parametro inexistente `new_owner_id` y esperaba `snapshot.ownerId`. La RPC versionada recibe `target_user_id` y devuelve el recibo de membresia plano con `targetUserId` y `membershipStatus`. El tramo aun no se habia ejecutado y no produjo escritura. | El cliente sintetico usa `target_user_id`, afirma el recibo plano y hace readback PostgreSQL independiente del owner y del estado Team. | La transferencia real devolvio el nuevo owner, el anterior perdio autoridad para apelar y la restriccion LIMITED permanecio intacta. La regresion prohibe `new_owner_id` y `snapshot.ownerId`. |
| W8B-019 | PRODUCT_BUG | fixed + regression_verified | La carrera autenticada entre `team.review.close` y `team.restriction.apply`, ambas basadas en la misma revision, produjo un unico ganador pero el perdedor recibio `P0002 query returned no rows` en lugar del conflicto autoritativo `STALE_REVISION`/`PT409`. El estado confirmado no se duplico, pero el error interno rompe el contrato de reconciliacion de clientes. | La RPC valida `reviewId`, traduce una revision inexistente a `TEAM_REVIEW_NOT_FOUND` y el arnes envia el ID canónico. La carrera conserva el lock y la comprobacion de revision antes de cualquier decision. | DB efimera, concurrencia local y staging autenticado pasan: un ganador, un `STALE_REVISION`, una unica revision 3, un solo receipt contendiente y refetch de dos sesiones al mismo snapshot. |
| W8B-TEST-020 | TESTABILITY_GAP | fixed + regression_verified | La primera regresion local de la carrera corregida afirmo que el agregado Team tendria exactamente dos recibos totales. El readback devolvio tres; contar todo el historial del agregado no demuestra si el intento perdedor escribio. El test fallo despues de confirmar revision 3 y no dejo la base efimera. | Se conservan los dos `operationId` contendientes y se cuenta exclusivamente sus recibos. | La carrera local devolvio un ganador, un `STALE_REVISION`, un solo recibo entre ambos operation IDs, revision 3 y cleanup completo de la base efimera. |
| W8B-TEST-021 | TESTABILITY_GAP | fixed + regression_verified | El readback consolidado calculo `bool_and(relrowsecurity)` sobre una mezcla de la invalidacion publica y cinco tablas del esquema `private`, y devolvio `false`. El contrato no exige RLS en tablas inaccesibles por esquema y ACL; la consulta mezclaba dos mecanismos de seguridad distintos y no demostro una apertura. | La evidencia se separa: RLS y policy para la invalidacion publica; aislamiento de esquema y ACL para las autoridades privadas. | Staging confirma RLS publica `true`, una policy, cero escritura autenticada; `anon/authenticated` sin `USAGE` privado, cero tablas privadas accesibles y lectura autenticada directa denegada. La regresion contractual enumera ambas capas. |
| W8B-SIM-022 | SIMULATION_BUG | fixed + regression_verified | La exportacion general de Demo World se regenero correctamente con ledger 212, pero el proof dedicado Team V3.1 seguia enlazado al authority hash bruto del ledger 204. La verificacion focal detecto tres divergencias explicitas: manifest, authority hash/conteo de migraciones y continuity source hash. No hubo escritura remota. | Team V3.1 enlaza ahora la continuidad al hash estable de la proyeccion semantica, no al digest bruto que contiene UUID y tiempos efimeros. Se regeneraron Team V3.1 y Demo World en ese orden y se congelaron manifest, proof y ledger 212 coherentes. | `demo-world:v31:verify` y `demo-world:v2:verify` pasan con cleanup completo, remote writes 0, projection hash `d4e7f2c6...` estable y `snapshotIdentical: true`; la suite focal pasa 31/31 y afirma que el source hash no puede ser el authority hash bruto. |
| W8B-ENV-023 | ENVIRONMENT_ISSUE | fixed + regression_verified | El preflight de variables Preview esperaba una clave moderna con prefijo `sb_publishable_`, pero el branch Supabase efimero expone el contrato publico legado `SUPABASE_ANON_KEY`. La validacion aborto antes de crear variables en Vercel y no imprimio valores. | Se uso la clave publica `anon` del mismo branch bajo `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, sin transportar `service_role`, con scope exclusivo `Preview` + `codex/team-operational-state-v1`. | El inventario Vercel muestra solo `NEXT_PUBLIC_SUPABASE_URL` y `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` en esa rama. Los 14 chunks cliente no contienen `SUPABASE_SERVICE_ROLE_KEY`, secretos Stripe/Supabase, URL PostgreSQL ni el project ref productivo; contienen una sola referencia al branch efimero. La QA autenticada contra ese branch volvio a terminar `TEAM_OPERATIONAL_STATE_V1_STAGING_PASS` con 9 identidades sinteticas, 11 dispositivos, Realtime canonico, cero PII y cero destinatarios externos. |
| W8B-TEST-024 | TESTABILITY_GAP | fixed + regression_verified | El primer baseline productivo de solo lectura supuso una columna generica `public.pachanga_competitions.metadata`; tras resolver sus columnas, un segundo intento supuso la relacion inexistente `public.pachanga_competition_matches`. PostgreSQL rechazo ambas consultas completas; no hubo escritura ni resultado parcial interpretado como valido. | Se resolvieron las columnas y relaciones desde `information_schema`. Los residuos QA se cuentan por los marcadores canónicos de Auth/Team y las competiciones por su FK real `organizer_group_id`; no se inventan columnas ni nombres de fixtures. | El baseline corregido y repetible confirma ledger 204, ultima `20260829152250`, 75 usuarios y 12 Teams preservados, y cero usuarios, Teams o competiciones sinteticas. Las migraciones seguian sin aplicar al cerrar la consulta. |
| W8B-ENV-025 | ENVIRONMENT_ISSUE | fixed + regression_verified | El primer intento de dry-run productivo fue rechazado localmente antes de crear procesos porque el trap de una configuracion temporal usaba `rm -rf`, operacion prohibida por la politica de herramientas. No se creo el directorio temporal ni se conecto a Supabase. | Se uso limpieza granular con `find -depth -delete`, sin cambiar `supabase/config.toml` ni relajar la politica local. | El dry-run productivo propuso exclusivamente las ocho migraciones `20260829221256` a `20260829221312`, termino con codigo 0 y el readback de `/tmp` confirma cero directorios `pachangas-wave8b-push.*`. |
| W8B-ENV-026 | ENVIRONMENT_ISSUE | fixed + regression_verified | La salida del lote secuencial de activacion se trunco en el cliente antes de mostrar los siete resultados finales. Repetir el lote a ciegas podia duplicar intenciones o producir conflictos de revision, aunque las RPC fueran idempotentes. | No se reintento ninguna operacion. Se leyo primero el read model canonico de settings y se continuo solo desde el estado confirmado por PostgreSQL. | El servidor devolvio revision 9, secuencia 21 y los ocho flags activos. El canary posterior paso sin cambiar esa revision ni esa secuencia. |
| W8B-ENV-027 | ENVIRONMENT_ISSUE | fixed + regression_verified | El binario local opcional `agent-browser` no estaba instalado al iniciar el smoke productivo. No se abrio ningun navegador alternativo ni se omitio QA. | Se reutilizo la conexion autorizada del navegador integrado y su control CDP ya inicializado para ejecutar la misma matriz. | Doce combinaciones de ruta y viewport, mas los siete estados de Demo World V3.1, terminaron sin overflow, imagenes rotas ni errores de consola. |
| W8B-ENV-028 | ENVIRONMENT_ISSUE | fixed + regression_verified | El primer parche documental intento borrar y volver a anadir el mismo informe dentro de una sola operacion `apply_patch`; la herramienta lo rechazo antes de escribir. | El informe se actualizo mediante un unico bloque de reemplazo y las incidencias se editaron en una operacion separada. | `git diff --check` y el diff documental final confirman dos rutas intencionales, sin cambios parciales ni archivos nuevos accidentales. |
| W8B-ENV-029 | ENVIRONMENT_ISSUE | fixed + regression_verified | La primera iteracion del smoke posterior al merge intento parsear una evaluacion CDP transitoriamente vacia tras navegar y el arnes termino con `undefined is not valid JSON`. La pagina estaba cargada, sin errores de consola, y no se produjo ninguna escritura. | La lectura comprobo primero que el resultado existiera y permitio reintentos acotados antes de parsearlo; no se considero la ausencia temporal como un resultado de producto. | La repeticion recorrio `/`, `/equipo/estado`, `/admin/teams` y Demo World en portrait; las cuatro evaluaciones respondieron en el primer intento, con DOM `complete`, cero overflow, cero imagenes rotas y cero errores de consola. |

La incidencia compartida `DW2-012` permanece registrada en `DEMO_WORLD_V2_REPORT.md` como `SIMULATION_BUG / fixed + regression_verified`; no se duplica como fallo nuevo de producto.

## Criterio de cierre

Cada fila debe terminar como `fixed + regression_verified`, o permanecer abierta con causa y efecto explicitos. Un fallo abierto que afecte autoridad, seguridad, privacidad, migraciones o cleanup bloquea el release.
